// Runtime configuration, injected before the app boots.
//
// In a container this file is regenerated at startup from the SEAM_CLOUD_URL
// environment variable (see packages/web/docker-entrypoint.sh). The committed
// version is intentionally empty: local dev leaves it blank and falls back to
// the build-time VITE_SEAM_CLOUD_URL instead.
window.__SEAM_CLOUD_URL__ = "";
